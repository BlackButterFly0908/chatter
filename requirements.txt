# RunPod serverless SDK
runpod

# Chatterbox TTS (Resemble AI)
chatterbox-tts

# Core ML dependencies (may be included with chatterbox-tts but explicitly listed)
torch
torchaudio

# Audio handling and processing
soundfile
numpy
scipy
librosa

# Optional watermark detection
perth

# HTTP + utils
requests
